Option Explicit

Sub test()
Dim str As String

str = "Many of us store our Macros in our Personal Macro workbook, but that leaves us" & _
" open to potentially losing 100's of macros that we use across a multitude of applications. " & _
" Well, in this video, we will cover how to export those macros to source control provider called GitHub. " & _
" By storing our macros on GitHub we can save them in a cloud-based application so that way if we lose our " & _
" personal Macro Workbook we don't lose our macros."

End Sub